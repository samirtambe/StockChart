# stockchart-cloud
A web application that queries Polygon.io for historical stock data and plots the data as a graph.  Stock queries are stored in a NoSQL database (DynamoDB).  Most queried stocks are also shown also.
